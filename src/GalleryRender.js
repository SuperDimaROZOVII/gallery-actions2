import React from "react";

function GalleryRender() {
    return(
        <>
        {/* {images.map(image => <GalImage id={image.id} url={image.url} favourited={image.favourited}/>)} */}
        </>
    )
}